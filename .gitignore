# OS / tooling
.DS_Store
.claude/
*.log
# Heavy source assets — NOT part of the deployed site (films stream from Bunny)
Olga/
Videos/
Splash/
Portraits/
Archive/
*.mov
*.mp4
*.pdf

# Prototype source-image folder (web copies live in scroll-assets/)
The team/

# Prototype source video folder (web copies live in scroll-assets/)
Videos/

# LAB gallery videos are web-optimized + committed (served by Netlify, like the LAB images)
!lab/video/*.mp4
